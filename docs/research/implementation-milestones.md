# 実装マイルストーン計画

[README.md](README.md) で合意した目的・ゴール・成果物と、
[lora-architecture-and-data-scale.md](lora-architecture-and-data-scale.md) §5.2 の判断ゲートを、
**実行可能なマイルストーンに落としたもの**。

各マイルストーンには **目的**(なぜやるか)と **ゴール**(何が達成されたら完了か)を
必ず一対で書く。ゴールは**測定可能な形**にし、満たせないときの**撤退条件**も併記する。
撤退条件を書いておかないとゲートが機能せず、予算を溶かす。

## 全体像

| # | マイルストーン | 対応ゲート | 実行場所 | コスト | 累計 | 実装の重さ |
|---|---|---|---|---:|---:|---|
| **M0** | 評価ハーネスとデータ棚卸し | G0 | **ローカル4090** | **$0** | $0 | 中 |
| **M1** | 学習基盤の実装 | — | **ローカル4090** | **$0** | $0 | **最重** |
| **M2** | 参照音声ICL注入の偵察 | G0.5 | **ローカル4090** | **$0** | $0 | 軽 |
| **M3** | 注入経路の機構学習(他話者データ) | A | ローカル + 必要ならクラウド | $0〜150 | $0〜150 | 重 |
| **M4** | 対象話者への適合 | B | **ローカル4090** | **$0** | $0〜150 | 軽 |
| **M5** | operating envelope の実測と model card | — | クラウド(並列生成) | $40〜120 | $40〜270 | 中 |
| **M6** | 日本語ディクション改善(副目標) | C | ローカル + クラウド | $50〜200 | $90〜470 | 重 |

**ローカル4090を主環境にしたことで、当初見積り($320〜860)から $90〜470 に下がった。**
費用が残るのは M5(条件数 × シード数で生成回数が多く、並列化の価値がある)と
M3 でバッチを上げたい場合だけ。詳細は次節。

- **主目標(M0〜M5)= $170〜560**
- **副目標込み(M0〜M6)= $320〜860** / 予算 $500〜1000 に収まる
- M1 は費用が小さいが**工数が最大**。ここが全体の律速。
- M6 は副目標なので、M5 までの結果と残予算を見て着手を判断する。

### 旧フェーズ名・ゲート名との対応

調査の過程で3つの命名が生まれているので対応を明示する。**現行の正は M0〜M6**。

| [custom-training-plan.md](custom-training-plan.md) §5(最初期) | [lora-...-data-scale.md](lora-architecture-and-data-scale.md) §5.2 | 本文書 |
|---|---|---|
| Phase 0(評価ハーネス) | G0 | **M0** |
| Phase 0.5(参照音声ICL注入) | G0.5 | **M2** |
| Phase 1(tokenizer head round-trip) | G0.5 に統合 | **M2** の完了条件2 |
| Phase 2(NAR注入アダプタ学習) | A | **M3** |
| Phase 4(per-voice 微調整) | B | **M4** |
| Phase 3(日本語 diction) | C | **M6** |
| —(新設) | — | **M1** 学習基盤の実装 |
| —(新設) | — | **M5** operating envelope の実測 |

### 依存関係

```
M0 ──┬─> M2 ──┐
     │        ├─> M3 ──> M4 ──> M5
     └─> M1 ──┘                  │
                                 └─> M6(残予算で判断)
```

- **M0 は全ての前提**。$0 なので最初に必ず終わらせる
- **M1 と M2 は並行できる**。M2 は学習を伴わないので M1 の完成を待たない
- M2 の結果は M3 の設計を絞り込むが、**M2 が失敗しても M3 は実行する**(§M2 のゴール参照)

---

## 実行環境 — ローカル RTX 4090 を主環境にする

### 判断(2026-09-18)

**ローカルの Windows + RTX 4090(24GB)を主環境とし、クラウドLinuxは限定用途に絞る。**
M0 / M1 / M2 / M4 はローカルで完結できる。ここが試行回数の大半を占める。

以前の記述(「作業機は macOS なのでローカルでは走らない」「80GB級が実質必須」)は
**調査に使った macOS 機**と**長窓・勾配チェックポイント無しの設計**に対する制約で、
短窓設計 + 4090 には当てはまらない。

### VRAM は足りる [推定]

学習(勾配チェックポイント有り、注入0.53M + LoRA r=16):

| 設定 | model | LoRA+Adam | 活性 | mask | 合計 |
|---|---:|---:|---:|---:|---:|
| S=1,000 × batch 8 | 6.67 | 0.23 | 2.38 | 0.03 | **9.31 GiB** |
| **S=1,000 × batch 16(推奨)** | 6.67 | 0.23 | 4.76 | 0.06 | **11.72 GiB** |
| S=2,000 × batch 8 | 6.67 | 0.23 | 4.76 | 0.12 | **11.78 GiB** |
| S=2,000 × batch 16 | 6.67 | 0.23 | 9.52 | 0.24 | **16.66 GiB** |

推論(`StaticKVCache` は **1トークンあたり 112 KiB** = 28層 × 8 KVヘッド × head_dim 128 × k/v × bf16)[コード]:

| シーケンス長 | CFG 1分岐 | CFG 2分岐(model 6.67 込み) |
|---|---:|---:|
| 4,500(3分の曲) | 0.48 GiB | 7.63 GiB |
| 12,036(1チャンク上限、§6.1 A3) | 1.29 GiB | 9.24 GiB |
| 24,576(context 上限) | 2.62 GiB | **11.92 GiB** |

**学習・推論いずれも24GBに倍の余裕がある。** WSL2 の予約分を引いても収まる。

### 速度差は許容範囲 [推定: FLOPs計算、MFU 30〜40%]

3,000 steps の所要時間:

| GPU | S=1,000 × bs 8 | S=1,000 × bs 16 |
|---|---:|---:|
| **RTX 4090** | **1.14〜1.52 h** | **2.28〜3.04 h** |
| A100 80GB | 0.60〜0.80 h | 1.20〜1.61 h |
| H100 SXM | 0.19〜0.25 h | 0.38〜0.51 h |

4090 は H100 の約6倍遅いが、1ランが1〜3時間なら実用的。

### クラウドの実コストは時間単価ではなくウォールクロック課金

純粋な学習計算はどちらでも安い:

| 試行回数 | H100($2.5/h) | A100($1.0/h) |
|---|---:|---:|
| 50ラン | $23.75 | $30.00 |
| 100ラン | $47.50 | $60.00 |
| 200ラン | **$95.00** | $120.00 |

**200ランでも$95。** クラウドの実コストは、デバッグ中にコードを読む時間・
データをアップロードする時間・エラーの原因を考える時間がすべて課金されること。
**M1 は試行錯誤が最も多いフェーズ**なので、ローカルで回せる価値が時間単価の差を上回る。

### README の「Linux」要件の正体 [コード]

コードレベルの POSIX 依存は **`fast`(vLLM)パスだけ**。

| 要素 | 状況 |
|---|---|
| 基本依存 | `torch` / `transformers` / `huggingface-hub` / `safetensors` / `tiktoken` / `numpy` / `soundfile` / `accelerate`。**すべてプラットフォーム非依存**(`pyproject.toml`) |
| `fast` extra | `vllm==0.19.0` + `triton==3.6.0`。**vLLM に Windows 公式 wheel は無い** |
| `fast.py` のコード | `os.killpg` + `signal.SIGTERM/SIGKILL`(`fast.py:215`/`:220`)、`selectors` でパイプを監視(`:236`/`:237`)。**POSIX 専用** |
| 読み込みタイミング | `pipeline.py:246`/`:288`/`:308` で**遅延 import**。`__init__.py` に "Optional backends are imported only when requested" |
| CUDA Graph / FP8 | `cuda_graph.py` / `quantization.py` に `sys.platform` 分岐は無い。FP8 は `get_device_capability() >= (8,9)` が条件で、**4090 は sm_89 なのでちょうど対象**(ただし FP8 時は CUDA Graph が無効化される、`sampling.py:86`) |

**README の「Linux」はサポート構成の宣言**であり、基本パスに OS 固有の実装依存はない。
ただし **Windows / WSL2 で実際に動かした検証はしていない**(静的に読んだだけ)[未検証]。

### 環境の選択

| 選択肢 | 評価 |
|---|---|
| **WSL2(推奨)** | Linux userspace でサポート構成に一致、CUDA は Windows ドライバ経由。`fast` の POSIX syscall も使える。**VRAM/RAM を少し予約される**点のみ注意 |
| ネイティブ Windows | 基本パスは動く見込みだが未検証領域。vLLM は使えない |
| クラウド Linux | 下記3ケースに限定 |

### クラウドを使う3ケース

| # | 場面 | 理由 |
|---|---|---|
| **1** | **M5(operating envelope の実測)** | 条件数 × シード数で生成回数が多い。4〜8 GPU を数時間借りて並列化する方が速い。ローカルは1GPU |
| **2** | **M3 で多話者データを大きく取る場合** | 設計上は24GBに収まるが、バッチを上げて収束を早めたいとき |
| **3** | **vLLM が必要になったとき** | AR生成の高速化。M5 の大量生成で効く。WSL2 でも動く可能性はあるが検証が必要 |

### M1 のリスクが下がる発見 — 密 attention mask は不要 [コード]

`nar.py` の `CachedNAR` は **密 mask を一切作らない**:

- `_prefill()` は AR のみで causal attention → `AR→AR causal` と `AR→NAR blocked` を自動的に満たす
- `velocity()` は NAR クエリを `cat(ar_k, nar_k)` に対して **マスクなしの全結合 attention** で解く
  → `NAR→AR full` と `NAR→NAR bidirectional` を満たす

つまり `nar_velocity()` の `[B,1,S,S]` 密行列(S=24576 で fp32 2.25 GiB)は
**リファレンス実装の都合**であり、2パス構成にすれば等価かつマスク不要。
§2.5 に挙げた M1 の障害のうち「密 mask」は消える。

さらに **AR側の K/V は `t` ごとに再計算不要**(`nar.py` が32 ODEステップ間で再利用しているのと同じ)。
学習でも潜在キャッシュと並べて事前計算でき、FLOPs が下がる。
**M1 の実装は `nar_velocity()` ではなく `nar.py` の構造を写すべき。**

---

## M0. 評価ハーネスとデータ棚卸し

### 目的

以降の**全ての判断の前提となる数値を確定する**。現在 §6.2 の B1〜B4 はすべて [推定] で、
「10分で足りるのか」「どの音域なら使えるのか」が数値になっていない。
また素のYuE2のベースラインを**分散込みで**取らないと、後で改善を主張できない
([community-implementations.md](community-implementations.md) が陥った
「差 0.006 がノイズに埋もれる」状態の再演を避ける)。

### ゴール

以下が**すべて数値として出ている**こと。

| 項目 | 出力形式 |
|---|---|
| 歌唱区間の実効秒数 | 曲ごと + 合計(無音・伴奏のみ区間を除外後) |
| F0 ヒストグラム | 半音ビン。**使用可能音域の上下限**が確定 |
| 母音 × 音域のセル被覆 | 日本語5母音 × 半音ビンの被覆率。**穴の位置が特定されている** |
| 強弱の分布 | ラウドネス(LUFS等)のヒストグラム |
| ミックス条件の一貫性 | 曲間の LTAS / スペクトル重心の差 |
| **stock ベースライン** | 素のYuE2で **10シード以上**生成し、話者照合コサインの**平均と標準偏差** |
| stock の PER | 4パスASRによる PER の平均と標準偏差 |

### 完了条件

1. 上記7項目が数値で出力され、レポートとして保存されている
2. **σ(標準偏差)が出ている**こと。平均値だけでは後続の「+2σ」判定ができない
3. `SongRequest.abc` に渡す旋律を、確定した音域内に収める手順が動く

### 撤退条件

**歌唱区間の実効秒数が 240 秒(4分)を下回った場合**、M3/M4 の見込みが大きく下がる。
その場合は M2(学習なし)に全振りし、M4 の容量をさらに下げる(注入層のみ 0.53M)。

### コストと成果物

- **$0**(完全にローカル完結)。評価系は手元の `whisper-large-v3` /
  `parakeet-tdt_ctc-0.6b-ja` / `anime-whisper` / `Qwen3-ASR-1.7B` /
  `pyannote/wespeaker-voxceleb-resnet34-LM` で足りる
- **stock ベースライン生成もローカル4090で走る**。3分の曲なら CFG 2分岐込みで 7.63 GiB
  (§実行環境)。10シードでも数十分
- 成果物: 計測スクリプト群、被覆レポート、ベースライン数値(σ込み)

---

## M1. 学習基盤の実装

### 目的

**公式が持っていない部分を作る。** YuE2 は推論専用リポジトリで、
`nar_velocity()` は `@torch.no_grad()` 付き・batch=1 前提・attention mask が密行列
([lora-architecture-and-data-scale.md](lora-architecture-and-data-scale.md) §2.5)。
flow-matching の学習ループも存在しない。**ここが全体の最大リスク**であり、
先に「学習が回る」ことを確定させないと M3/M4 の失敗原因を切り分けられない。

### ゴール

**素のモデルで flow-matching loss が意図通り下がる**ことが、
以下の3つのテストで確認されている。

| テスト | 内容 | 合格条件 |
|---|---|---|
| **T1 ゼロ初期化同値性** | zero-init 注入層を挿した状態で推論 | 出力が素のモデルと**数値的に一致**(bit-exact または 1e-5 以内) |
| **T2 単一バッチ過学習** | 1曲の1区間だけを与えて意図的に過学習させる | loss がほぼ 0 まで落ち、その区間の潜在を**再構成できる** |
| **T3 決定性の非破壊** | 学習コードを通した後で通常推論を実行 | 同一シードで同一出力。リポジトリの再現性の作り込み([custom-training-plan.md](custom-training-plan.md) §6.2)を壊していない |

**T2 が最重要。** これが通らない限り、後続で loss が下がらなくても
「実装が悪いのか、手法が悪いのか」が判別できない。

**実装は `nar_velocity()` ではなく `nar.py` の `CachedNAR` の構造を写す。**
`CachedNAR` は密 attention mask を作らず、AR を causal で prefill して K/V をキャッシュし、
NAR クエリを `cat(ar_k, nar_k)` に対してマスクなし全結合 attention で解くことで
ハイブリッドマスクと等価な結果を得ている(§実行環境の末尾)。
これにより §2.5 に挙げた障害のうち「密 mask」が消え、
**AR側 K/V を `t` ごとに再計算しない**分 FLOPs も下がる。

### 完了条件

1. T1 / T2 / T3 がすべて合格
2. 20秒窓 × batch 16 が **約12 GiB に収まる**ことを実測で確認(§3.2 の推定値の検証)
3. 潜在キャッシュ(VAEエンコード結果の事前計算)が動き、再生成手順が文書化されている
4. 設定がシリアライズされ、同一設定から再実行できる(成果物が重みなので再現性は要件)

### 撤退条件

**T2 が通らない状態が続く場合**、`t` のサンプリングが推論側 `_shift_t_value`
(sigmoid + `timestep_shift=1.0`)と整合していない可能性が最も高い。
ここを2回作り直しても通らなければ、**M2(学習なし)の結果だけで
プロジェクトの継続を判断する**。学習基盤が作れなければ成果物(重み)は作れない。

### コストと成果物

- **$0**(ローカル4090で完結)。試行錯誤が最も多いフェーズなので、
  ウォールクロック課金のクラウドではなくローカルで回すこと自体が最大の節約になる
- 参考: 3,000 steps が 4090 で **1.14〜1.52 h**(S=1,000 × batch 8)。
  デバッグランはこれより短い
- 成果物: 学習ループ、潜在キャッシュ、AR K/V キャッシュ、T1〜T3 のテスト、設定シリアライザ

---

## M2. 参照音声ICL注入の偵察

### 目的

**学習を1ステップも回さずに、音色が乗るかどうかを判定する。**
実装は約20行(低レベル関数 `generate_tokens` / `nar.synthesize` を直叩きし、
対象話者の semantic token を AR 文脈に prepend する)。
効けば M3 の学習目標が「機構をゼロから作る」から
「効いた注入の効き方を重みに固定する」に変わり、**M3/M4 のリスクが大きく下がる**。

なお公開APIは prefix を厳格に検証するため(`pipeline.py:277` / `:293`)、
**低レベル関数を直接使う**必要がある。

### ゴール

**「効いた / 効かなかった」がσ付きで確定していること。** どちらでも M2 は完了。

| 結果 | 判定 | M3 への影響 |
|---|---|---|
| 話者照合コサインが M0 のベースライン **+2σ 以上** | **効いた** | M3 の学習目標が「効いた注入を重みに固定する」に変わる。難易度が下がる |
| +2σ に届かない | **効かなかった** | M3 は機構をゼロから学習する。当初計画どおり |

**「効かなかった」は失敗ではない。** M2 の目的は判定であって改善ではない。

### 完了条件

1. 上記判定が **3シード以上**の平均と σ で出ている
2. コミュニティ tokenizer head v9 による対象話者のトークン化が動いている
3. **PER が壊れていないこと**も同時に測る(注入で歌詞が崩れるなら注入位置が悪い)

### 撤退条件

tokenizer head v9 の round-trip(音声→token→再構成)が聴感で別人になる場合、
上流のノイズが支配的で判定にならない(v9 は top-1 一致 18.9% / top-5 45.0%)。
その場合は **M2 を「判定不能」として閉じ、M3 に進む**。M2 に追加投資しない。

### コストと成果物

- **$0**(ローカル4090。学習を伴わないので生成と評価のみ)
- 成果物: ICL注入スクリプト、判定レポート(σ込み)、tokenizer head の round-trip 評価

---

## M3. 注入経路の機構学習(他話者データ)

### 目的

**「参照音声から音色を読む」機構そのものを獲得する。**
この機構の学習には**対象話者のデータを1秒も使わない**(§4.7.2)。
対象話者の10分は 34.87M の LoRA には過剰度 55.9 倍で足りないが、
機構は他話者データで学べるので、10分を「条件入力」として使えるようになる。
**これが10分固定という制約への本質的な回答**。

### ゴール

**学習に使っていない話者で、参照音声を差し替えると出力の音色が変わる**こと。

| 測定 | 合格条件 |
|---|---|
| 未学習話者2名以上で参照を差し替え | 話者照合コサインが**参照した話者の方向に動く**(参照Aで生成 → Aとの類似 > Bとの類似) |
| 伴奏品質 | 生成物の伴奏が崩れていない(§4.5 の領域ミスマッチが出ていない) |
| PER | 素のモデルから悪化していない |

**「対象話者に似たか」はM3では測らない。** ここで測るのは**機構が汎化したか**だけ。

### 完了条件

1. 上記3測定が合格
2. **cross-song ペアリング**で学習している(同一区間ペアだとコピーで loss が下がり音色を学ばない、§2.4)
3. 学習データの規模が実測されている(jaCappella の曲数・総時間は
   HF API の `viewer: false` により未取得のため、ダウンロード時に実測が必要)

### 撤退条件

**未学習話者で参照差し替えが効かない場合**(コサインが参照方向に動かない)、
機構は汎化していない。**注入層を4本から増やす / 層位置を変える**を各1回試し、
それでも動かなければ M3 を閉じて **M4 を「対象話者への直接適合」として実行**する。
その場合 §6.4 の D 表どおり **operating envelope の条件は一切緩まない**ので、
M5 で条件を厳しく記載する。

### コストと成果物

- **$0〜150**。ローカル4090で回せる(S=1,000 × batch 16 で 11.72 GiB、2.28〜3.04 h/3,000 steps)。
  多話者データを増やしてバッチを上げ、収束を早めたい場合のみクラウドを使う
- 成果物: 注入経路の重み、学習設定、汎化評価レポート

---

## M4. 対象話者への適合

### 目的

**対象話者の個別性を入れる。** M3 で獲得した機構の上に、
対象話者10分ぶんの適合を最小容量で載せる。
容量は **注入 0.53M + LoRA r=8 / MLPのみ / 14層 = 計 3.28M**(過剰度 5.3 倍、§4.7.3)。

### ゴール

**README のゴール定義(3条件)を満たすこと。**

| # | 条件 | 測定 |
|---|---|---|
| 1 | 話者照合コサインが stock を上回る | **leave-one-song-out(4〜6 fold)の全fold平均**。単一 held-out・単一シードでは判定しない |
| 2 | PER が悪化していない | 4パスASR。M0 のベースライン ± σ 以内 |
| 3 | §6 の operating envelope 条件下で成立 | A1(音域拘束)/ A2(タグ一致)/ A4(サンプリング)を守った生成で測る |

### 完了条件

1. 上記3条件が LOO-CV で満たされている(4 fold × 600 steps で $0.25、§4.7.6)
2. 窓5秒・ランダムオフセット・600〜1,500 steps で学習している
3. リミックス拡張を適用し、ピッチシフト/フォルマント変換は**使っていない**(§4.7.5)
4. **重みが保存され、M1 の設定シリアライズから再現できる**

### 撤退条件

**LOO-CV 全fold平均で stock を上回れない場合**、容量・step数・層位置を各1回振って再試行。
それでも届かなければ、**M5 で「主ゴール未達」を明記した上で、
達成できた範囲(Lv1 の歌い回しなど)を成果物として確定させる**。
無条件に試行を続けない。

### コストと成果物

- **$0**(ローカル4090)。4 fold × 600 steps は 4090 でも1時間未満
- 成果物: **対象話者アダプタ重み(3.28M)**、LOO-CV レポート、学習設定

---

## M5. operating envelope の実測と model card

### 目的

**§6 の条件表を「予測」から「測定結果」に変える。**
現在 §6 の条件のうち [コード] 裏付けは A3(8分)と A1 の実施手段だけ、
[実測] はジャンル一致とサンプリング設定だけで、残りは全部 [推定]。
成果物が重みである以上、**条件を知らずに範囲外で使われると「効かない」と評価される**ので、
動作範囲の実測は成果物の一部。

### ゴール

**§6 の A / B / C / D 各条件が実測値で裏付けられ、model card が書ける状態。**

| 条件 | 実測すること |
|---|---|
| A1 音域 | 音域を段階的に外していったとき、**どこでコサインが崩れるか**の境界値 |
| A2 タグ・ジャンル | 一致 / 不一致でのコサイン差(コミュニティの 0.909 vs 0.885 を自分の環境で再測) |
| A3 尺 | 8分超で2チャンクに割ったときの境界での音色ドリフト量 |
| A4 サンプリング | temperature / top_k / rep penalty のグリッドでの最良点 |
| A5 シード | best-of-n の n をいくつにすれば安定するか(n=1,3,8 での成功率) |
| B1〜B3 | M0 の被覆穴の位置と、実際に崩れた位置の**対応** |
| D | M3 が成功したか否かで、A1/B1〜B3 がどれだけ緩んだか |

### 完了条件

1. 上記が数値で出ている
2. **model card が書かれている**: 推奨音域(半音)、推奨タグ、推奨尺、推奨サンプリング設定、
   制限事項(C1〜C5)、ライセンス(CC BY-NC 4.0 の派生物、非商用)
3. §6 の各行の `[推定]` ラベルが `[実測]` に置き換わっている(置き換わらなかった行は明記)

### 撤退条件

なし。**M4 が主ゴール未達でも M5 は実行する**。
「どこまでは効いて、どこからは効かない」を残すことが成果物の価値になる。

### コストと成果物

- **$40〜120**。**唯一クラウドを積極的に使うマイルストーン**。
  条件数 × シード数で生成回数が多く、4〜8 GPU を数時間借りて並列化する価値がある
  (ローカルは1GPUなので直列)
- 成果物: **model card**、条件別の実測レポート、§6 の更新

---

## M6. 日本語ディクション改善(副目標)

### 目的

**日本語の歌詞追従(PER)を改善する。** 公式ベンチ(`docs/benchmarks.md`)は
WildSongBench 192プロンプト / 17設定で**言語別内訳を公開していない**ため、
日本語PERは誰も測っていない空白地帯([japanese-support.md](japanese-support.md))。
また `SongRequest` に `phonemes` フィールドが無く、
入力は書記素のまま Qwen BPE に通される。

### ゴール

**4表記(漢字 / ひらがな / カタカナ / ローマ字)の PER 比較で最良の入力形式が確定し、
かつ AR LoRA で PER が素のモデルより改善している**こと。

| 段階 | ゴール |
|---|---|
| **M6a(無料)** | 同一歌詞を4表記で入力して PER を比較。**最良表記が σ 付きで確定** |
| **M6b(学習)** | 日本語ディクション AR LoRA を **full temporal coverage** で学習し、最良表記での PER が M6a を上回る |

**M6a は学習不要**なので、M6b に進まなくても単独で価値がある(運用時の入力形式が決まる)。

### 完了条件

1. M6a: 4表記 × 3シード以上の PER が σ 込みで出ている
2. M6b: AR LoRA が **各シーケンスの全区間を loss に通している**
   (コミュニティ実装の「先頭ウィンドウのみ」欠陥を踏まない、§4.4)
3. 日本語のモーラ処理(っ / ん / ー)がアライメントで壊れていない

### 撤退条件

**M6a で表記間の PER 差が σ 以内**だった場合、入力形式は結果に影響していない。
その場合 M6b の投資対効果が下がるので、**残予算を M5 の条件実測に回す**。

### コストと成果物

- **$50〜200**(M6a は**ローカルで $0**、M6b が大半)。
  M6b もローカルで回せるが、AR LoRA は窓を広げる必要があるため
  メモリに余裕を持たせたい場合はクラウド
- 成果物: 4表記比較レポート、日本語ディクション AR LoRA 重み(M6b まで進んだ場合)

---

## 予算の配分と打ち切り方針

| シナリオ | 実行範囲 | 費用 |
|---|---|---:|
| 順調(M2 または M3 が効く) | M0〜M5、残予算で M6 | $90〜470 |
| M3 失敗・M4 のみ成功 | M0〜M5(条件を厳しく記載) | $40〜270 |
| M1 が通らない | M0 + M2 で判断を確定させて停止 | **$0** |
| M4 主ゴール未達 | M5 まで実行し、達成範囲を成果物化 | $40〜270 |

**どのシナリオでも予算 $500〜1000 に大きく余裕がある。**
M1 が通らないシナリオは**ローカル完結なので費用ゼロ**で判断できる。
予算はボトルネックではなく、**M1 の実装工数がボトルネック**。
余った予算は M5 の条件実測(生成回数を増やしてσを小さくする)と
M6 の日本語 AR LoRA に回すのが効率的。

## まだ決まっていないことの影響

[README.md](README.md) の未確定4件が、この計画に与える影響。

| 未確定 | この計画への影響 |
|---|---|
| 日本語の位置づけ(副目標か主目標か) | **主目標なら M6 を M3 の前に繰り上げる**。M6a は $0〜30 なので M0 と同時でもよい |
| 成功の最終判定者(指標か耳か) | 耳が最終なら M0 の評価ハーネス構築を軽くし、M5 の聴感評価を厚くする |
| 対象話者と権利関係 | M1〜M5 の内容は変わらない。M5 の model card の**公開範囲の記載**が変わる |
| 期間 | 短ければ M6 を切る。M0〜M5 は分割不可(M1 を飛ばすと成果物が作れない) |

## 未検証の前提

| # | 前提 | 検証方法 | 外れた場合 |
|---|---|---|---|
| 1 | **Windows / WSL2 で YuE2 が動く** | M0 の最初に `python examples/generate.py` を1回走らせる | ネイティブWindowsが駄目ならWSL2、WSL2も駄目ならクラウドLinuxに全面移行($320〜860の当初見積りに戻る) |
| 2 | 学習時VRAMの推定値(S=1,000×bs16 で 11.72 GiB) | M1 で実測(完了条件2) | バッチを半分に落とす。S=1,000×bs8 なら 9.31 GiB |
| 3 | 4090 の MFU 30〜40% | M1 の最初のランで s/step を実測 | 遅ければ step 数を減らすか FP8 を検討(4090 は sm_89 で対象。ただし CUDA Graph が無効化される) |
| 4 | jaCappella の曲数・総時間 | M3 の着手時にダウンロードして実測 | 小規模なら他の多話者ソースを追加 |
